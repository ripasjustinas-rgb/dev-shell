#!/usr/bin/env sh
set -eu

if ! command -v checkupdates >/dev/null 2>&1; then
  printf '%s\n' '—'
  exit 0
fi

# checkupdates reads the sync DB but does not acquire pacman's transaction lock.
count=$(checkupdates 2>/dev/null | wc -l)
printf '%s\n' "$count"
