# Hyprpaper 0.8 shows a Hyprland splash while it decodes a newly requested
# image. Wallpaper changes are initiated by laptopui, so never expose that
# placeholder between images.
splash = false

# Enable the native wallpaper control socket used by `hyprctl hyprpaper`.
ipc = true
