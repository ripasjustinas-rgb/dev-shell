# Let Kitty provide the translucent surface and wallpaper blur.
# Btop keeps its graphs, text and highlighted rows, but does not paint a
# separate opaque background.
theme_background = false

# Use the terminal's true-colour palette for the rest of the interface.
truecolor = true
